{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build App",
            "type": "shell",
            "command": "cmake --build build --target main",
            "group": "build"
        },
        {
            "label": "Build Tests",
            "type": "shell",
            "command": "cmake --build build --target tests",
            "group": "build"
        }
    ]
}